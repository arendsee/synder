SYNMAP_COLS <- c(
  "qconid" = "character",
  "qstart" = "integer",
  "qstop"  = "integer",
  "tconid" = "character",
  "tstart" = "integer",
  "tstop"  = "integer",
  "score"  = "numeric",
  "strand" = "character"
)

GFF_COLS <- c(
  "conid"   = "character",
  "source"  = "character",
  "type"    = "character",
  "start"   = "integer",
  "stop"    = "integer",
  "score"   = "numeric",
  "strand"  = "character",
  "phase"   = "integer",
  "seqname" = "character"
)

CON_LENGTH <- c(
  "conid"  = "character",
  "length" = "integer"
)

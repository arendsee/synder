SYNMAP_COLS <- c(
  "qcon"   = "character",
  "qstart" = "integer",
  "qstop"  = "integer",
  "tcon"   = "character",
  "tstart" = "integer",
  "tstop"  = "integer",
  "score"  = "numeric",
  "strand" = "character"
)

GFF_COLS <- c(
  "con"     = "character",
  "source"  = "character",
  "type"    = "character",
  "start"   = "integer",
  "stop"    = "integer",
  "score"   = "numeric",
  "strand"  = "character",
  "phase"   = "integer",
  "seqname" = "character"
)

CON_LENGTH <- c(
  "con"    = "character",
  "length" = "integer"
)

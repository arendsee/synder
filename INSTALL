To compile call:

```
make
```

Currently this is no where near production level so I do not have any handling
for installation.

To build the document pages call:

```
make docs
```

To return the folder to its original state call:

```
make clean
```
